import 'package:flutter/material.dart';

class FavoriteApge extends StatelessWidget {
  const FavoriteApge({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(backgroundColor: Colors.amberAccent);
  }
}
