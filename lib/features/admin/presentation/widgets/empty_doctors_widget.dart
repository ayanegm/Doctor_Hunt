import 'package:flutter/material.dart';

class EmptyDoctorsWidget extends StatelessWidget {
  const EmptyDoctorsWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(children: [Text('No Doctors ')]);
  }
}
